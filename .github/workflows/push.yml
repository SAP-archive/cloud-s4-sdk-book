name: Build and push

on:
  push:
    branches:
      - '**'
  workflow_dispatch:

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        ref: consumer-test
    - name: Run CI
      run: |
        echo ${{ secrets.DOCKERHUB_PASSWORD }} | docker login -u ${{ secrets.DOCKERHUB_USER }} --password-stdin
        docker build -t fwilhe/cf-it:latest .
        docker push fwilhe/cf-it:latest
